<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar <%=products.name %></title>
    <link rel="stylesheet" href="/css/Register.css">
    <link rel="icon" href="/img/Fondo.png">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
</head>

<body>
    <header>
        <%-include('../partials/header.ejs')%>
    </header>
    <main class="register">
        <div class="registro">
            <h3 class="registro">Editar producto: <%= products.name %></h3>
            <div class="formulario">
                <form action="/products/edit/<%= products.idProduct %>" method="POST" enctype="multipart/form-data">
                    <div class="registro">
                        <input type="text" name="name" id="name" placeholder="Nombre de producto" required minlength="2"
                            value=<%=products.name %>>
                        <input type="number" name="price" id="price" placeholder="Precio" required
                            value=<%=products.price %>>
                        <input type="number" name="discount" id="discount" placeholder="Descuento" required
                            value=<%=products.discount %>>
                        <input type="text" name="category" id="category" placeholder="Categoria" required
                            value=<%=products.category %>>
                        <input type="text" name="description" id="description" placeholder="Descripcion" required
                            value=<%=products.description %>>
                        <input type="file" name="image" id="image" placeholder="Imagen">
                    </div>
                
            </div>
        </div>

        <div class="buttons">
            <input type="submit" value="Editar Producto" class="btn">
        </div>
                 </form>
        <!-- <button type="submit">Editar!</button> -->
                
    </main>

    <footer>
        <%-include('../partials/footer.ejs')%>
    </footer>
</body>

</html>